#version 410 core

layout (location = 0) in vec2 vertexPos;
layout (location = 1) in vec2 texCoord;

uniform mat4 gWorld;

out vec2 TexCoord0_CS;

void main() {
    gl_Position = gWorld * vec4(vertexPos.x, 0, vertexPos.y, 1);
    TexCoord0_CS = texCoord;
}