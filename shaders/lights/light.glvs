#version 330

layout (location = 0) in vec3 vertexPos;
layout (location = 1) in vec2 TexCoord;
layout (location = 2) in vec3 vertexNormal;
layout (location = 3) in vec3 vertexTangent;

uniform mat4 gWVP;

void main() {
    gl_Position = gWVP * vec4(vertexPos, 1.0);
}