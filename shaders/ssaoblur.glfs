#version 140

uniform sampler2D input;

const int uBlurSize = 4;

in vec2 UV;

out vec4 output;

void main() {
   vec2 texelSize = 1.0 / vec2(textureSize(input, 0));
   float result = 0.0;
   vec2 hlim = vec2(float(-uBlurSize) * 0.5 + 0.5);
   for (int i = 0; i < uBlurSize; ++i) {
      for (int j = 0; j < uBlurSize; ++j) {
         vec2 offset = (hlim + vec2(float(i), float(j))) * texelSize;
         result += texture(input, UV + offset).w;
      }
   }
 
   output = vec4(texture(input, UV).rgb, result / float(uBlurSize * uBlurSize));
}